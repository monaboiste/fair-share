plugins {
    id("formatting-conventions")
}

spotless {
    groovy {
        target("src/*/groovy/**/*.groovy")

        leadingTabsToSpaces(4)
        trimTrailingWhitespace()
        endWithNewline()
    }
}
